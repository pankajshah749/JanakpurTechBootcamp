import { Bot, Braces, Code2, Database, Layers3, Rocket, Sparkles } from "lucide-react";

const iconMap = {
  bot: Bot,
  braces: Braces,
  code: Code2,
  database: Database,
  layers: Layers3,
  rocket: Rocket,
  sparkles: Sparkles,
};

export default iconMap;
